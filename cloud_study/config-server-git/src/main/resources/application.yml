spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: http://git.oschina.net/didispace/config-repo-demo/
#          search-paths: repo
#          username:
#          password:

server:
  port: 1201

# 配置服务注册中心
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:1001/eureka/